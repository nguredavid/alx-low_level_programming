#include <stdio.h>
/**
 * main - entry pt
 * Return: reverse output
 */
int main(void)
{
	char alph;
	int num;

	for (alph = 'a'; alph <= 'f'; alph++)
	{
		putchar(alph);
	}
	for (num = 0; num < 10; num++
		{
	    putchar((num % 10) + '0');
		}
		return (0);

}
