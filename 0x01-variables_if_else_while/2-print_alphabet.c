#include <stdio.h>
/**
 * main - entry point
 * Return: return 0
 */
int main(void)

{
	char = alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)

	{
	putchar(alpha);
	putchar('\n');
	}
	i
	return (0);
}
